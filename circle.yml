machine:
# Version of ruby to use
  ruby:
    version: 2.0.0-p648
  environment:
    JAVA_OPTS: "-Xms512m -Xmx768m"
  timezone: Asia/Saigon
dependencies:
  cache_directories:
    - ~/.android
checkout:
  post:
    - scripts/saddler.sh
test:
  override:
    - scripts/saddler.sh
  post:
    - echo $CIRCLE_TEST_REPORTS
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type d -regex ".*/reports/tests$" -exec cp -r {} $CIRCLE_TEST_REPORTS/junit/ \;
#deployment:
#  master:
#    branch: master
#    commands:
#      - ./gradlew assembleDebug uploadDeployGate -PdisablePreDex